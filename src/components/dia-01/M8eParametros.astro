---
import EjemploReal from '../slides/EjemploReal.astro';
import Cita from '../ui/Cita.astro';
import Nota from '../ui/Nota.astro';
import Notas from '../ui/Notas.astro';
---

<EjemploReal modulo="D1-M8e" titulo="Un parámetro, no una ruta escrita a mano" icono="sliders-horizontal">
  <div class="flex flex-col gap-8">
    <Cita mono>{`// RutaDatos: parámetro Texto, sin barra final
File.Contents(RutaDatos & "\\fact_produccion.csv")

// Para listar una carpeta (otra operación):
Folder.Contents(RutaDatos)`}</Cita>
    <Nota>
      Si la ruta está escrita directo en cada consulta, cambiar de computadora rompe
      las ocho consultas a la vez. Con un parámetro, se cambia en un solo lugar.
    </Nota>
  </div>
  <Notas
    slot="notas"
    minutos={6}
    dice="Un parámetro es una casilla que Power Query trata como constante en todas las consultas que lo usan. Hoy les ahorra reescribir la ruta ocho veces; en planta les va a ahorrar reescribirla cada vez que IT mueva la carpeta de servidor."
    nota="Inicio → Administrar parámetros → Nuevo: RutaDatos, tipo Texto. En cada consulta sustituir únicamente la ruta dentro de File.Contents, conservando Csv.Document y sus opciones. Las funciones M se escriben en inglés incluso con la interfaz en español."
  />
</EjemploReal>
