---
interface Props {
  mono?: boolean;
}
const { mono = false } = Astro.props;
---

<blockquote
  class:list={[
    'text-5xl leading-snug font-titulo font-bold text-center px-16',
    { 'font-codigo text-4xl': mono },
  ]}
>
  <slot />
</blockquote>
